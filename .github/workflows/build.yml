# Copyright (C) 2026 Banten-IT-Solutions https://github.com/Banten-IT-Solutions
# https://github.com/ophub/amlogic-s9xxx-openwrt

name: Build OpenWrt
on:
  workflow_dispatch:
    inputs:
      board_version:
        description: 'Select device to compile'
        required: true
        default: 'HG680-P'
        type: choice
        options:
          - HG680-P
          - B860H
      rootfs_size:
        description: 'Select Rootfs Size'
        required: true
        default: '1024'
        type: choice
        options:
          - 256
          - 512
          - 1024
          - 1536

jobs:
  build:
    name: 🚀 Build ${{ github.event.inputs.board_version }}
    runs-on: ubuntu-22.04
    steps:
      - name: 📦 Checkout
        uses: actions/checkout@v4

      - name: 🧬 Clone Builder
        run: |
          git clone --depth 1 https://github.com/ophub/amlogic-s9xxx-openwrt.git

      - name: 🩹 Patch Overlay
        run: |
          mkdir -p amlogic-s9xxx-openwrt/config/imagebuilder/files
          cp -r files/files/. amlogic-s9xxx-openwrt/config/imagebuilder/files/
          printf 'CONFIG_PACKAGE_luci-theme-material=y\n' > amlogic-s9xxx-openwrt/config/imagebuilder/config

      - name: 🏷️ Resolve Version
        run: |
          VER=$(curl -s https://downloads.openwrt.org/releases/ | grep -oE 'href="[0-9]+\.[0-9]+\.[0-9]+/"' | grep -oE '[0-9]+\.[0-9]+\.[0-9]+' | sort -V | tail -n1)
          echo "VER=$VER" >> $GITHUB_ENV
          echo "Resolved OpenWrt version: $VER"

      - name: 🍞 Bake Rootfs
        run: |
          sudo apt-get update -y >/dev/null
          sudo apt-get install -y zstd make >/dev/null
          cd amlogic-s9xxx-openwrt
          chmod +x config/imagebuilder/imagebuilder.sh
          ./config/imagebuilder/imagebuilder.sh openwrt:${{ env.VER }}

      - name: 📥 Stage Rootfs
        run: |
          mkdir -p amlogic-s9xxx-openwrt/openwrt-armsr
          cp amlogic-s9xxx-openwrt/output/*rootfs.tar.gz amlogic-s9xxx-openwrt/openwrt-armsr/

      - name: 🎯 Resolve Board
        run: |
          case "${{ github.event.inputs.board_version }}" in
            HG680-P) BOARD=s905x ;;
            B860H)   BOARD=s905x-b860h ;;
          esac
          echo "BOARD=$BOARD" >> $GITHUB_ENV

      - name: ⚙️ Compile
        run: |
          cd amlogic-s9xxx-openwrt
          sudo ./remake -b ${{ env.BOARD }} -s ${{ github.event.inputs.rootfs_size }}

      - name: 🔍 Locate Image
        run: |
          FILE_PATH=$(find ./amlogic-s9xxx-openwrt/openwrt/out/ -name "*.img.gz")
          if [[ -z "$FILE_PATH" ]]; then
            echo "Error: Tidak ada file firmware ditemukan!"
            exit 1
          fi
          echo "FILE_PATH=$FILE_PATH" >> $GITHUB_ENV

      - name: 🕒 Stamp Date
        id: set_date
        run: |
          export TZ='Asia/Jakarta'
          echo "date_time=$(date +'%Y-%m-%d')" >> $GITHUB_ENV

      - name: 🚀 Publish
        uses: softprops/action-gh-release@v2
        env:
          GITHUB_TOKEN: ${{ secrets.TOKEN_GITHUB }}
        with:
          name: BITS-WRT ${{ env.VER }} (${{ github.event.inputs.board_version }})
          tag_name: v${{ env.VER }}_${{ github.event.inputs.board_version }}
          draft: false
          prerelease: false
          files: |
                ${{ env.FILE_PATH }}
          body: |
            ### BITS-WRT Information
            * Default IP: 192.168.1.1
            * Default username: root
            * Default password: bitswrt
            * Default WIFI name: BITS-WRT
            * Default WIFI password: 12345678
            * Release date: ${{ env.date_time }}